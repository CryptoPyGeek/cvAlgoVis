{
  "name": "cvalgovis-desktop",
  "version": "0.1.0",
  "private": true,
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:win:portable": "electron-builder --config electron-builder.yml --win portable",
    "build:win:dir": "electron-builder --config electron-builder.yml --win dir",
    "pack:win": "npm run build:win:portable"
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "wait-on": "^8.0.1"
  }
}
