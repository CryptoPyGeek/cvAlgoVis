{
  "name": "cvalgovis-workspace",
  "private": true,
  "scripts": {
    "build:web": "npm --prefix frontend run build",
    "build:backend": "python -m pip install -r backend/requirements.txt && pyinstaller backend/pyinstaller.spec --distpath backend/dist --workpath backend/build",
    "build:desktop": "npm run build:web && npm run build:backend && npm --prefix desktop/electron install && npm --prefix desktop/electron run build"
  }
}
